@model List<TravelTrip.Models.Concrete.Blog>

<div class="box1-left">
    @foreach (var item in Model)
    {
    <div class="bot-img">
        <a href="/Blog/BlogDetay/@item.BlogId"><img style="width:120px;height:80px" src="@item.BlogImage" alt="" /></a>
    </div>
        <div class="bot-desc1">

            <p>@item.Description.Substring(0,100)</p>
        </div>
        <div class="clear"></div>
    }
</div>
